#include <bits/stdc++.h>
using namespace std;

bool solve(vector<vector<bool>> board,string word,int row,int col){
    if(word.empty()) return true;
    if(row==board.size() && col==board.size()) return false;
    
}

bool exist(vector<vector<char>>& board, string word) {
    
}

int main(){
    
    return 0;
}